version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 16
      commands:
        - echo Installing source NPM dependencies...
        - npm install
  post_build:
    commands:
      - aws elasticbeanstalk update-environment --environment-name
        test-environment
